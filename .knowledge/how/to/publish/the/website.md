---
status: "green"
revised_at: "2026-09-22T11:05:46+10:00"
---

The site is a plain HTML/CSS GitHub Pages site for `realdavidfarrell.com`. The remote is `https://github.com/linkin-parks-bassist/linkin-parks-bassist.github.io.git`. GitHub Pages currently uses the `main` branch at repository root (`build_type: legacy`, `source: {branch: main, path: /}`). To publish reviewed local site changes, commit only the intended site files and push `main` to `origin`; then check the Pages build/deployment status and the live URL. Review untracked files before staging. Include the local `.knowledge/` tree when David explicitly asks to commit everything and its leaves have been checked for public content; otherwise stage only the intended site files. Provenance: repository `README.md`, `CNAME`, `git remote -v`, and GitHub `gh api repos/linkin-parks-bassist/linkin-parks-bassist.github.io/pages` checked 2026-09-22. The Pages settings and remote are mutable, so recheck before a future publication.
