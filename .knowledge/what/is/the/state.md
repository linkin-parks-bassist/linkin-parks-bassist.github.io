---
status: "green"
revised_at: "2026-09-22T10:55:10+10:00"
---

The Website currently has three dated Field Notes for 21 September 2026, listed in reverse chronology on `index.html` and `field-notes/index.html`: `petalinux101.html`, `jev-jacobian-correction.html`, and `jev-jacobian.html`. The two Jev pages use the PetaLinux note's navigation, wide prose, page-level date, grounded narrative voice, and right-aligned square end mark. The first now tells the original experiment in its prior-to-correction voice, with only the top banner linking to the later correction. It starts with David's Yes/Maybe/No Jev CLI and `where` option, the three Maybe/No/Maybe prompts, the original terminal screenshot, his reaction, and his request for Codex to script the 500-map run. The follow-up explains the determinant confound and the matched 48.8% result. Downloadable pilot, first 500-map, and matched 500-map JSON files are in `field-notes/`. Counts and links were checked against the saved benchmark project; `git diff --check` passed; Chromium screenshots at desktop and mobile widths were inspected on 2026-09-22. The pages, indexes, shared style, and three JSON downloads were committed as `7a5c65a` and pushed to `origin/main` on 2026-09-22. GitHub Pages reported that commit built successfully; the live homepage, Field Notes index, both Jev pages, and matched-results JSON returned HTTP 200 with the expected content and table order at `https://realdavidfarrell.com/`. Since that publication, David has locally edited both Jev pages. The correction page now has a short Codex/David transcript from his screenshots after the “JC had fallen” sentence, styled with the homepage's dark terminal treatment in `style.css`. Desktop and mobile Chromium screenshots were checked and `git diff --check` passed. These later edits to both Jev pages and `style.css`, plus `field-notes/jev-jacobian-first-runs.png`, remain uncommitted and are not on the live site. The revised first note and PNG were checked in desktop and mobile Chromium renders, its only correction link is in the top banner, and `git diff --check` passed.

`field-notes/petalinux101.html` remains the accepted first Field Notes style reference, committed earlier as `b8e6335`. It is the account of David's first KV260 PetaLinux 2025.2 image build, ending at generated artifacts rather than hardware boot. It documents the Codex/PetaLinux methodology, David's hands-on work and questioning, the agent's research and drafting, Knowledgetrees' three roots, the central image-content questions, and the misleading nonzero build exit followed by successful artifact inspection. The rootfs manifest showed 250 packages including systemd, BusyBox, OpenSSH, Bash and iproute2; generated output included `Image`, `system.dtb`, `image.ub`, rootfs archives, ext4 filesystem, and a package manifest. The host AppArmor user-namespace setting was restored to its original value 1. The existing Website `.knowledge/` tree remains untracked in Git.

The sibling `jev-jacobian-bench` project's plan, state, and next leaves were corrected to treat the matched result as current. Its benchmark files were not changed.
